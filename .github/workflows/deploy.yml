# GitHub Pages deployment for Essentials of Paleomagnetism JupyterBook
#
# INSTRUCTIONS:
# 1. Uncomment the entire workflow below when ready to deploy
# 2. Go to repo Settings > Pages > set Source to "GitHub Actions"
# 3. Push to main branch to trigger deployment
#
# NOTE: This workflow is commented out because the repo is currently private.
# Uncomment when the repo goes public and you're ready to deploy.

# name: Deploy JupyterBook to GitHub Pages
#
# on:
#   push:
#     branches:
#       - main
#   workflow_dispatch:
#
# permissions:
#   contents: read
#   pages: write
#   id-token: write
#
# concurrency:
#   group: "pages"
#   cancel-in-progress: false
#
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#
#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: "3.11"
#
#       - name: Install dependencies
#         run: |
#           pip install jupyter-book
#
#       - name: Build the book
#         run: |
#           cd book && jupyter book build
#
#       - name: Upload artifact
#         uses: actions/upload-pages-artifact@v3
#         with:
#           path: book/_build/html
#
#   deploy:
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#     runs-on: ubuntu-latest
#     needs: build
#     steps:
#       - name: Deploy to GitHub Pages
#         id: deployment
#         uses: actions/deploy-pages@v4
